## 1. Host-neutral geometry (`coords`)

- [x] 1.1 Create `python/otio_sync_core/coords.py` with `DEFAULT_ASPECT_HALF = 8/9`, `DEFAULT_SPACING = 0.8`, `DEFAULT_FONT_SIZE`
- [x] 1.2 Implement `aspect_half(width, height)` returning `W/(2H)`, falling back to `DEFAULT_ASPECT_HALF` when height is 0/missing
- [x] 1.3 Implement `px_to_otio(px, py, W, H)` and `otio_to_px(x, y, W, H)` normalizing by **height**, Y-up, centre-origin
- [x] 1.4 Unit tests: `aspect_half(1920,1080)==8/9`; `aspect_half(1920,0)==DEFAULT_ASPECT_HALF`; centre pixel → `(0,0)`; px↔otio round-trip stable
- [x] 1.5 Confirm no host-specific unit constants (RV font/width factors) leak into `coords`

## 2. Shared shape tessellation (`shapes`)

- [x] 2.1 Create `python/otio_sync_core/shapes.py` with host-neutral ellipse/rectangle/arrow → OTIO-normalized polyline functions
- [x] 2.2 Move the tessellation math out of `xs_annotation_codec.py` (currently lines ~227-284) into `shapes.py`; re-point the xStudio codec at it
- [x] 2.3 Unit test tessellation output equals the pre-refactor xStudio point output (behavior-preserving)

## 3. RV codec — pure conversion + contract

- [x] 3.1 Create `python/otio_sync_core/rv_annotation_codec.py` with `HOST_ID="rv"`, `SUPPORTED_KINDS`, `RV_FONT_SCALE=5000.0`, `RV_WIDTH_SCALE=0.6`, `font_size_to_rv`/`rv_to_font_size`
- [x] 3.2 Implement `sync_events_to_rv_specs(events, ctx)` as a pure function (no `rv.commands` import) returning ordered `PaintNodeSpec` dicts
- [x] 3.3 Dispatch on `event.schema_name()` (never `isinstance`); handle `PaintStart`/`PaintPoint(s)`/`PaintEnd`, `TextAnnotation`, `EllipseAnnotation`, `RectangleAnnotation`, `ArrowAnnotation`
- [x] 3.4 Emit the full property superset per kind (pen/erase, text, ellipse/rect, arrow) per the rv-annotation-codec spec
- [x] 3.5 Route kinds not in `SUPPORTED_KINDS` through the shared `shapes` tessellation fallback
- [x] 3.6 Implement `rv_specs_to_sync_events(read_props)` reverse conversion, preserving text `scale` (as `rv_strokes_to_sync_events`)
- [x] 3.7 Add `to_sync_events` / `from_sync_events` contract entry points wrapping the above
- [x] 3.8 Unit tests (outside RV): double-loaded schemadef still classifies `PaintStart`; `TextAnnotation.scale` round-trips; each kind produces its full prop set

## 4. RV applier — the thin `commands` edge

- [x] 4.1 Implement `apply_specs(specs, commands, *, rv_node, frame, mode)` in `rv_paint_applier.py` — the only `rv.commands`-touching function; absorbs the duplicated `set_prop` helper (`user` is per-spec, not a call kwarg)
- [x] 4.2 `mode="append"`: create fresh nodes, allocate strokeids, (re)build the frame `order` list
- [x] 4.3 `mode="reconcile"`: match by `uuid` in `order`, update in place when found, add when not, prune managed nodes whose uuid is absent from the incoming set (reproduce `annotation_sync.py:664-759`)
- [x] 4.4 Reject unknown `kind` values loudly rather than silently skipping

## 5. Migrate testchart batch (parity gate)

- [x] 5.1 Replace the inline SyncEvent→paint rendering in `testchart/batch_openrv_helper.py` with `sync_events_to_rv_specs` + `apply_specs(mode="append")`
- [x] 5.2 Replace inline `px_to_norm` / `font_size/5000` sites with `coords` / `rv_annotation_codec` helpers (the `font_size/5000` site moved into the codec via `font_size_to_rv`)
- [x] 5.3 **Absolute gate** — ran the testchart batch; `compare_testchart` PASS on `vector_colors` (all 8 arches, sub-pixel); `compare_thickness` numbers on `vector_thickness` identical to pre-migration baseline
- [x] 5.4 **Regression gate** — `idiff` on all 12 `rv_output/*.png` vs `rv_output.old/*`: 10/12 clean PASS. The 2 `vector_fonts*` frames differ only because this RV build's `rvio` SIGABRTs on font frames (`QFontDatabase` construction order bug) and falls back to a screen-resolution GUI grab — reproduced identically pre- and post-migration, confirmed as a pre-existing RV/rvio limitation, not a codec regression

## 6. Migrate OTIO load plugin (both directions)

- [x] 6.1 Rewrite `ori_annotations_plugin.py::import_annotations` to use `from_sync_events` + `apply_specs(mode="append")` (drops the `isinstance` matching). Extended `rv_strokes_to_sync_events` to also cover ellipse/rect/arrow reverse conversion (net-new capability — the legacy `export_annotations` silently dropped shapes entirely) and added `rv_paint_applier.read_frame_strokes` as the read-side counterpart to `apply_specs`
- [x] 6.2 Rewrite `ori_annotations_plugin.py::export_annotations` paint→SyncEvent parsing to use `read_frame_strokes` + `rv_strokes_to_sync_events`
- [x] 6.3 Separate the GUI (QFileDialog) from the codec logic: added `_import_annotations_from_file(otiofile)` and `_export_annotations_to_directory(basepath, ...)` headless methods, called by the menu-bound `import_annotations`/`export_annotations` after gathering dialog input (parity with the xStudio plugin surface)
- [x] 6.4 Verified live in RV via the properly reinstalled package (`rvplugin/ori_annotations/reinstall.csh` — see [[feedback_rvpkg_reinstall_before_test]]): import→read-back round-trip for clip 0 (frame 1) produces the correct 12 pen strokes (matching the original), confirming the codec/applier plumbing is behavior-preserving through the actual installed plugin. **Discovered pre-existing bug (not caused by this migration, not fixed here)**: `import_annotations`/`export_annotations` treat `ReviewItemFrame.frame` as the RV global sequence frame, but it is source-local — only the first clip in a multi-clip OTIO round-trips correctly (clips 2+ get written under the wrong frame key). Confirmed by reproducing identical `int(frame.frame)` semantics as the pre-migration original. See [[project_import_annotations_multiclip_frame_bug]] — tracked as a separate follow-up, out of scope for the codec unification

## 7. Migrate live-sync renderer

- [x] 7.1 Rewrite `rvplugin/ori_sync/annotation_sync.py`'s fresh-stroke receive writers (`_apply_annotation`, `_apply_text_annotation`, `_apply_shape_annotation`) to build specs via `sync_events_to_rv_specs` + write via `apply_specs`. `_apply_annotation_render`'s TextAnnotation branch (previously a manual dup-uuid scan + fallthrough to `_apply_text_annotation`) collapsed into one `apply_specs(mode="reconcile")` call, since reconcile already subsumes "find by uuid, update in place, else add." Deleted the now-dead `_text_uuid_exists_in_rv`. Added `rv_paint_applier.read_stroke` (single-item counterpart to `read_frame_strokes`) and used it to migrate the SEND-side reader `_construct_annotation_events`
- [x] 7.2 Rewrote `_apply_annotation_replace` fully onto `apply_specs(mode="reconcile")` (find/update/add/prune, filtering out PaintStart/PaintPoints/PaintEnd — strokes are intentionally untouched by a "replace"). `_apply_shape_annotation`'s manual skip-if-duplicate-uuid check replaced by reconcile's update-in-place (a safe superset: idempotent for true dupes, correctly applies an edit if the data changed). `_apply_partial_annotation`'s call into `_apply_annotation` updated (dead `join`/`cap`/`hold`/`ghost` params removed); its own in-place incremental-point-update branch (existing partial stroke) deliberately left bespoke — see 7.4
- [x] 7.3 `_import_existing_rv_annotations` needed no direct rewrite — it delegates to `_broadcast_annotation` → `_construct_annotation_events`, already migrated in 7.1
- [x] 7.4 Resolved: partial/in-progress strokes do NOT fit the shared IR and were deliberately left as bespoke direct-property updates (`_apply_partial_annotation`'s existing-pen in-place branch) — this is genuinely a different operation (incremental point/width update on an already-live node, mid-stroke) from the IR's "describe a complete node" model, and the design's own risk section anticipated this
- [x] 7.5 Live-validated single-host (via `reinstall.csh` + a focused RV script — pen uuid freshness, text uuid persistence) **and** a real two-host (xStudio+RV) session run by the user. The two-host pass found and fixed two real pre-existing bugs surfaced by exercising the codec migration live, both unrelated to the migration itself (confirmed by identical pre/post-migration frame-math): (1) [[project_clip_source_range_none_fallback]] — xStudio→RV annotations on timecode-bearing clips landed on the wrong paint node; `_find_paint_node_for_media`/`_clip_guid_for_media_and_frame` only checked `clip.source_range`, missing the legitimate `source_range=None` + `media_reference.available_range` case; (2) [[project_no_timecode_start_frame_off_by_one]] — xStudio displayed one frame late on non-timecode media, because `sequence_sync.py::_make_clip` embedded RV's synthetic `startFrame=1` (no-timecode convention) literally into the exported `available_range` instead of normalizing it to OTIO's 0-based convention. Both fixed, regression-tested (8 new tests total), reinstalled, and confirmed working by the user live. **Still not covered by this pass** (see follow-up test list): shapes (ellipse/rect/arrow) live sync in either direction, the text "replace"/edit reconcile path, delete/prune of annotations, and a cut sequence with repeated media occurrences
- [x] 7.6 **Design decision during migration** (user directive): `hold`/`ghost`/`ghost_before`/`ghost_after` are legacy fields removed from the sync path entirely — no longer read from SyncEvents on receive, no longer read from RV properties into broadcasts on send. RV pen nodes still get `hold`/`ghost`/`ghostBefore`/`ghostAfter` properties created, but now always as fixed local defaults (0), never sync-derived — they are local RV display concerns, not part of the sync schema. The shared `SyncEvent.py` schemadef itself was NOT touched (a separate, more invasive decision) — the fields simply stopped being populated/consumed by this code path
- [x] 7.7 **Discovered pre-existing divergence, preserved deliberately, not fixed**: `_apply_text_annotation` passes through the *real* font value from received events (unlike the batch/load-plugin paths, which hardcode `font=""` per `rv_annotation_codec._text_spec`). Handled via a per-call-site override of the codec's `font` prop rather than changing the shared `_text_spec` (which would have risked the already-validated Group 5/6 parity baselines)

## 8. xStudio coords wiring + contract retrofit

- [x] 8.1 Updated `xs_annotation_codec.py`: `spacing=0.0` → `coords.DEFAULT_SPACING` (0.8) and both `50.0` font-size fallbacks → `coords.DEFAULT_FONT_SIZE` in `xs_captions_to_sync_events`/`sync_events_to_xs_captions`. `coords.DEFAULT_ASPECT_HALF` not applicable here — `aspect_half` is always a required caller-supplied parameter in this file, never internally defaulted
- [x] 8.2 Updated `xstudio_plugin/ori_annotations/ori_annotations.py::_aspect_half_for_media` to call `coords.aspect_half()` / return `coords.DEFAULT_ASPECT_HALF`, replacing the inline `img_w/(2*img_h)` + literal `0.8889`/`1920,1080` fallbacks
- [x] 8.3 Added `HOST_ID="xstudio"`, `SUPPORTED_KINDS={"pen","erase","text"}` (shapes excluded — already tessellated into strokes internally, not natively rendered), and `to_sync_events`/`from_sync_events` wrapping the native `{"strokes","captions"}` dict shape that already matches `Bookmark.set_annotation(...)`'s kwargs
- [x] 8.4 Found the "stale" coordinate comment already correct (says "by image height", not width — already fixed by an earlier commit). Real fix: `testchart/generate_testchart.py` calls `annotation_builder.px_to_norm`/`norm_to_px` (not `coords` directly) at 7 call sites — rather than rippling an import rename, made `annotation_builder.px_to_norm`/`norm_to_px` thin delegates to `coords.px_to_otio`/`coords.otio_to_px`, so every existing call site now runs through `coords` as the single source of truth with zero call-site changes
- [x] 8.5 **Regression gate (xStudio)** — ran the real xStudio batch (`testchart/batch_xstudio.py`) through the migrated plugin; `idiff` on all 24 output frames (12 charts × normal/UHD) vs `xstudio_output.old/*` → **100% clean PASS**, better than anticipated. The spacing fix (0.0→0.8) is confirmed applied in the exported OTIO's `TextAnnotation.spacing` field (verified directly: old=0.0, new=0.8) but produces **zero pixel diff**, because xStudio's own caption widget has no "spacing" concept at all (`sync_events_to_xs_captions`'s xStudio caption dict carries no such key) — spacing only affects glyph kerning in RV's text renderer, so the visual effect would only appear if this exported OTIO were later rendered in RV, not in this xStudio-only render pipeline

## 9. Text comparison harness

- [x] 9.1 Added `--mode text` to `compare_testchart.py` for the `vector_fonts.png` chart. Ground truth (7 labels, matching `generate_testchart.py::vector_fonts_annotations()`) is stored OTIO-normalized and resolved per-image via `coords.otio_to_px` — UHD needs no separate scale factor since H-normalized coords are resolution-independent. **Revised from the spec's symmetric-window design**: text is left-baseline anchored (extends up+right from the anchor, not centered), so a small square window misses large glyphs entirely (ascent for 96pt exceeds 100px). Uses an asymmetric scan (sized from the known per-label font pixel height, generous rightward run) and reports the offset of the *leftmost* matching-colour column from the expected anchor — "does the text start where expected," not a 2D centroid
- [x] 9.2 Pass/fail at ±5px, scaled by resolution for UHD (matching how colour-arch mode already scales; a "5px" error at 1x is a "10px" error at 2x, not a stricter failure). Wired into the overall batch exit code
- [x] 9.3 Added two invocations to `run_batch.csh` (xStudio + OpenRV) alongside the existing colour-registration calls, same `.00000.png`/`_DEFAULT_MEDIA` filename conventions
- [x] 9.4 **(added)** Found and fixed a real bug during validation: initial weights `[1.0,-0.3,-0.3]` (copied from `RED arch`) scored the chart's near-white background as a false match, since arch charts have dark backgrounds but this one is near-white (245,245,240). Fixed to `[1.0,-0.5,-0.5]` (a true "redness" differential, R − (G+B)/2). Caught by a new synthetic-image unit test suite (`tests/otio_sync/test_compare_testchart_text.py`, 6 tests, no RV/xStudio dependency) — the blank-image "not found" case is what exposed it; the live spot-check against a real xStudio render passed under both old and new weights and would not have caught this alone
- [x] 9.5 **Validated against real renders**: xStudio 1080p and UHD (`testchart/xstudio_output`) — all 7 labels PASS on both. OpenRV — correctly reports FAIL, but this is the pre-existing [[project_rvio_font_crash]] issue (RV's font frames fall back to a 704×483 screen-grab, not a bug in this comparison code); confirmed the tool behaves correctly on a known-bad input rather than false-passing. **Follow-up needed, not done here**: genuine RV text validation requires re-running the batch against `/Applications/OpenRV.app` (3.1.0, "old" — has working text but predates ellipse/rect/arrow shape support) instead of the custom 4.0.0 build; deferred pending user direction since old RV may not handle the shape-annotation frames

## 10. Validation & spec sync

- [x] 10.1 Ran the full combined batch (`run_batch.csh`: generate → xStudio batch → OpenRV batch → thickness ×2 → colour ×2 → text ×2), fresh end-to-end with all current fixes in place. Both batches completed with `SUCCESS`/no tracebacks (only the pre-existing, harmless `$OCIO` env warning). Colour registration: PASS both. Thickness: numbers match established baselines exactly (no regression). Text: xStudio 7/7 PASS; OpenRV FAIL — confirmed the known [[project_rvio_font_crash]] screen-grab-fallback artifact (704×483, not a real render), not a regression — exactly the "as good as it can be until openrv fixes text" state the user confirmed
- [x] 10.2 Grep-confirmed: `5000.0` has zero remaining inline occurrences. `0.8889`/`w*0.6` had two small gaps, both fixed: `xstudio_plugin/ori_annotations/ori_annotations.py::_bookmark_to_sync_events`'s default parameter now uses `coords.DEFAULT_ASPECT_HALF`; the two `w * 0.6` literals in `annotation_sync.py`'s deliberately-bespoke partial-stroke path (task 7.4 — left unmigrated on purpose) now reference `rv_annotation_codec.RV_WIDTH_SCALE` instead of a duplicated literal, without changing that path's bespoke structure. Remaining `0.8889` mentions are docstring examples only (e.g. "≈ 0.8889"), not code
- [x] 10.3 Grep-audited all four RV call sites for inline `rv.commands` paint-node property get/set outside `rv_annotation_codec`/`rv_paint_applier`. `batch_openrv_helper.py` and `ori_annotations_plugin.py`: clean (only unrelated media-path/output-size reads remain). `annotation_sync.py` has the most hits; each classified: (1) `order_prop` reads in `_import_existing_rv_annotations` (line 199) and `_broadcast_frame_annotations_replace` (line 932) are discovery/iteration only — they read the item-name list to find *what* to process, then hand off to the already-migrated `_construct_annotation_events`/codec for the actual structural read, not a duplicate; (2) lines 296/303/470/474/794 are the documented bespoke partial-stroke path (task 7.4, deliberately left unmigrated); (3) lines 705/712-713 are the documented D11 hold/ghost local-default writes; (4) lines 893/896-897/903 are the documented D9 uuid-ensure-persist + soft-delete-skip pre-processing in `_construct_annotation_events`, needed caller-side before invoking the codec. `playback_sync.py`: zero hits, fully clean. No un-migrated structural duplication found
- [x] 10.4 Reconciled all delta specs against implemented behavior. `otio-annotation-sync` (aspect-scale transform, font-scale symmetry, scale round-trip) and `xstudio-annotation-export` (caption scale=1.0, font factor) both already matched code exactly — no text changes needed, but found and fixed one real code gap surfaced while verifying: `xs_annotation_codec.py` had two inline `2.5` font-factor literals (lines ~177, ~438) instead of a named constant, contradicting the spec's "not inline constants" language; added `XS_FONT_SCALE = 2.5` mirroring the `RV_FONT_SCALE` pattern (152 tests still pass). `annotation-coord-transform` spec matched implementation as-is. `rv-annotation-codec` spec had three real drifts, all fixed: (1) referenced a nonexistent `rv_specs_to_sync_events` function — actual name is `rv_strokes_to_sync_events` (fed via `rv_paint_applier.read_stroke`/`read_frame_strokes`); (2) described `PaintNodeSpec.props` as 3-tuples `(name, rv_type, value)` — actual shape is 4-tuples with a trailing `dim`; (3) described `apply_specs`'s signature with a `user` kwarg that doesn't exist — `user` actually lives per-entry on each `PaintNodeSpec`, and the real optional kwarg is `start_id`. `testchart-text-comparison` spec was the most stale: it still described the original *symmetric centred window + centroid* design, which task 9.1 explicitly revised to an *asymmetric ascent-biased leftmost-ink-column scan* once left-baseline text anchoring was understood — rewrote all three scenarios to match the shipped `analyse_text_label` implementation
- [x] 10.5 Added a "Release note" section to `proposal.md` documenting the xs-originated caption spacing change (0.0 → 0.8): xs captions previously rendered with collapsed/illegible letter spacing in RV; now render correctly-spaced. No repo-wide CHANGELOG exists, so this lives alongside the change's own proposal (same location as the existing `scale` round-trip reconciliation note) for whoever writes release notes when this ships

## 11. Packaging & import resilience

- [x] 11.1 Guard the eager sync-API imports in `otio_sync_core/__init__.py` so leaf imports (`coords`, `shapes`, `rv_annotation_codec`, `rv_paint_applier`) work without the network/pika stack (offline batch + annotations-only rvpkg)
- [x] 11.2 Decouple `rv_annotation_codec` from `otio_sync_core.manager` (inline the dict-schema fallback) so the annotations-only package needs no manager/network/pika
- [x] 11.3 Add the 4 new modules to `rvplugin/ori_sync/makepackage.csh` zip list
- [x] 11.4 Bundle the leaf codec into `rvplugin/ori_annotations/` — add zip section + `otio_sync_core` folder entry to its `PACKAGE` (`location: Python`)
- [x] 11.5 Rebuilt and reinstalled both rvpkgs via `reinstall.csh` (`ori_annotations`, `ori_sync`); confirmed the installed `~/Library/Application Support/RV/Python/ori_annotations_plugin.py` and bundled `otio_sync_core/*` are byte-identical to the repo source, and the plugin imports/runs correctly in real RV — note: edit only `rvplugin/...`, never the `_site/` Jekyll copies
