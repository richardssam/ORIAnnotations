## 1. RV send-trigger plumbing

- [x] 1.1 In `python/otio_sync_core/inspection.py`, add `_REGISTERED_ANNOTATION_CONTROLLER`, `register_annotation_controller(controller)`, and `get_registered_annotation_controller()`, mirroring the existing `_REGISTERED_MANAGER`/`register_manager`/`get_registered_manager` triplet.
- [x] 1.2 In `rvplugin/ori_sync/plugin.py`, call `otio_sync_core.register_annotation_controller(self.annotation)` immediately next to the existing `otio_sync_core.register_manager(self.sync_manager)` call (~line 253).

## 2. OpenRV hook: `draw_annotation` action

- [x] 2.1 In `sync_test/python/sync_test/openrv_hook.py::execute_openrv_command`, add `elif action == "draw_annotation":` dispatching on `payload.get("kind")` (`"pen"` or `"rect"`).
- [x] 2.2 Implement the `pen` case: resolve the target paint node for the current view/frame (reuse the same lookup pattern `add_media`/`set_selection` use to find the active sequence), write `brush`, `color`, `width` (from `payload["width"]`), and `points` (a short fixed 2-point stroke) directly via `rv.commands.newProperty`/`setFloatProperty` — not via `rv_paint_applier.apply_specs`.
- [x] 2.3 Implement the `rect` case: same node resolution, write `min`, `max`, `borderColor`, `innerColor`, `borderWidth` (from `payload["border_width"]`) directly.
- [x] 2.4 For both cases, append the new item to `{node}.frame:{frame}.order` using the `"{prefix}:{strokeid}:{frame}:{user}"` format and next-id logic imported from `otio_sync_core.rv_paint_applier` (do not re-derive the format ad hoc).
- [x] 2.5 After writing, call `otio_sync_core.get_registered_annotation_controller()._broadcast_annotation(node_name, component)` to trigger the real send path. Return `{"action": "draw_annotation", "status": "success", "node": node_name, "component": component}`.
- [x] 2.6 Raise a clear error if `get_registered_annotation_controller()` returns `None` (plugin not registered yet) rather than failing with an `AttributeError`.

## 3. OpenRV hook: state readback extension

- [x] 3.1 In `openrv_hook.py`'s state getter, for each counted stroke also call `otio_sync_core.rv_paint_applier.read_stroke` and include each stroke's `width` (pen) or `size` (rect, already `borderWidth * 2.0` per `read_stroke`) in the `annotations` list entries. (Used `read_stroke` per-component directly rather than `read_frame_strokes`, since the existing loop already enumerates components per node/frame via `rv.commands.properties`.)
- [x] 3.2 Keep the existing `annotation_count` field computed exactly as today — additive change only.

## 4. xStudio hook: `draw_annotation` action

- [x] 4.1 In `sync_test/python/sync_test/xstudio_hook.py::execute_xstudio_command`, add `elif action == "draw_annotation":` handling `kind == "pen"` (reject/no-op with a clear error for `"rect"`, per design D3 — xStudio has no wired-up native shape broadcast path).
- [x] 4.2 Resolve the bookmark target for the current frame (reuse the existing session/bookmark lookup pattern already used by the state getter) and call `bm.set_annotation(strokes=[{...}])` with a stroke dict shaped like a native xStudio pen stroke, using `payload["thickness"]` as the nominal native thickness.
- [x] 4.3 Do not call any plugin-internal function — rely on the plugin's own poll loop (`flush_pending_annotations`) to pick up and broadcast the bookmark. Return `{"action": "draw_annotation", "status": "success"}` immediately after the write (convergence waiting happens in the test, not the action).

## 5. xStudio hook: state readback extension

- [x] 5.1 In `xstudio_hook.py`'s state getter, alongside the existing `ann_state["strokes"]`/`["captions"]` counts, include each `pen_strokes` entry's `thickness` (and `size` if present) in the `annotations` list entry (the raw `data.get("pen_strokes", [])` is already being read at that point — just stop discarding the geometry).

## 6. Test cases and assertion helper

- [x] 6.1 Add a small helper (`sync_test/python/sync_test/annotation_assertions.py`) that computes the expected peer-side width/thickness by calling `rv_annotation_codec` functions/constants directly (per design D3/D4) and asserts `assertAlmostEqual`-style tolerance against the observed `/state` readback. Note: the xStudio-side formula (`size / (2 * aspect_half)`) is a stable, non-tunable mathematical identity with no standalone named function in `xs_annotation_codec.py` to import, unlike `RV_WIDTH_SCALE` — it's reproduced directly rather than copy-pasting a magic number. Also includes `XSTUDIO_ANNOTATION_CONVERGENCE_TIMEOUT`, hand-duplicated from the plugin's `DEBOUNCE_SECONDS`/`ANNOTATION_SCAN_INTERVAL` (see note in the file: those modules import xStudio's `xstudio.core` at module scope, unavailable to this out-of-process harness, so a live import wasn't feasible — flagged as a deviation from D2/D4's "not hardcoded" framing).
- [x] 6.2 Add a `draw_annotation` command type to the script-driven command vocabulary documented in `sync_test/README.md` (`add_media`, `set_selection`, ... table), matching the `pen`/`rect` payload shapes from tasks 2 and 4.
- [x] 6.3 Add test cases to `sync_test/sync_tests.yaml`: `openrv_draws_pen_xstudio_verifies`, `xstudio_draws_pen_openrv_verifies`, `openrv_draws_rect_xstudio_verifies`, each `script_driven: true` with a `commands` list ending in `draw_annotation`. Refinement discovered during implementation: the per-test assertion can't be a generic checkpoint (RV/xStudio annotation geometry has different shapes, `width` vs `stroke_thickness`, and `compare_states` already deliberately excludes `annotations`/`annotation_count` from structural equality for exactly this reason), so added a new optional `annotation_geometry` yaml block + `Runner._verify_annotation_geometry` (in `runner.py`, using `annotation_assertions`) rather than trying to force it through the existing generic checkpoint mechanism. Not duplicated into `sync_tests_xstudio.yaml` — that file has diverged from `sync_tests.yaml` (different app pairings for some same-named tests), so blindly copying entries risked guessing at its intent; flagging rather than guessing.
- [x] 6.4 Wire the convergence wait for the xStudio-driven case to `annotation_assertions.XSTUDIO_ANNOTATION_CONVERGENCE_TIMEOUT` (see that file for why this is a hand-duplicated constant, not a live import, per the note added under task 6.1) via `_verify_annotation_geometry`'s own bounded poll — independent of the yaml's `convergence_wait`, so the yaml entries don't need an inflated fixed wait.

## 7. Verification

- [x] 7.1 Initially could not run live (no display/GUI in the implementation session). **The user then ran `openrv_draws_rect_xstudio_verifies` live themselves** (`./run_tests.sh run --test openrv_draws_rect_xstudio_verifies`), which surfaced the real master-race bug documented in design.md D5 and task 8 below. After that fix, the user re-ran it and it **passed live**: `sync_test/logs/openrv_draws_rect_xstudio_verifies/runner.log` shows `✅ Annotation geometry round-trip verified: {'driver': 'openrv', 'peer': 'xstudio', 'kind': 'rect', 'nominal': 1.0}`, `✅ Apps agree on timeline structure (full-state consensus)`, `✅ Test 'openrv_draws_rect_xstudio_verifies' PASSED.`. The other two (`openrv_draws_pen_xstudio_verifies`, `xstudio_draws_pen_openrv_verifies`) have not been run live yet — no log directory exists for either as of this writing.
- [x] 7.2 Verified at the codec-unit-test layer (`test_rv_annotation_codec.py::test_pen_width_forward_reverse_roundtrip`, added when the bug was originally fixed) — still passes. The live integration re-break/re-fix demonstration (temporarily dropping the `/ RV_WIDTH_SCALE` fix and confirming `openrv_draws_pen_xstudio_verifies` fails) has not been done live yet, since that specific test hasn't been run at all (see 7.1) — remains a follow-up.
- [x] 7.3 **Answered — live.** `openrv_draws_rect_xstudio_verifies` passed on the first successful live run: OpenRV's `border_width: 1.0` round-tripped to xStudio's tessellated-stroke `thickness` within tolerance (expected `1.125`, per `expected_xstudio_thickness_from_rv_border_width(1.0)`). This is a real signal that the box-shape *data* round-trip (`_box_shape_spec`'s `size / 2.0` and its `read_stroke` inverse, paired with `xs_annotation_codec`'s tessellation-thickness formula) is internally consistent end-to-end — i.e. no asymmetric-scale bug of the same shape as the pen-width one. It does **not** by itself answer whether the *rendered/visual* thickness looks right in each app (a separate, rendering-calibration question, out of scope per design.md's Non-Goals) — only that the underlying data pipe preserves the value correctly.
- [x] 7.4 Ran the full existing `tests/otio_sync/` suite before and after this change's edits (including after the D5 `manager.py`/`runner.py` additions): same 4 pre-existing failures both times (an unrelated in-progress selection-sync feature, plus one test-ordering-sensitive test — both confirmed via `git stash` earlier in this session to predate this work), no new failures.

## 8. Master-race fix (found via live testing, see design.md D5)

- [x] 8.1 Add `is_master` as an extra top-level key on `SyncManager.export_state()`'s payload (`python/otio_sync_core/manager.py`) — confirmed inert for existing structural comparisons (`project_state`/`diff_states` only read named `StateSnapshot` fields).
- [x] 8.2 Surface `is_master` on the lightweight `/state` endpoint for both apps: `openrv_hook.py` reads it directly off the registered manager (in-process); `xstudio_hook.py` reads it opportunistically from the existing `ORI_FULLSTATE_FILE` bridge (out-of-process).
- [x] 8.3 Add `"is_master"` to `Runner.compare_states`' `ignore_keys` (`runner.py`) since it legitimately differs between peers by design.
- [x] 8.4 Add `Runner._wait_for_master(port, timeout=15.0)` and call it before a script-driven test's command loop, logging a clear diagnostic (not a hard failure) if the driver doesn't hold master within the timeout.
- [x] 8.5 **Live-verified**: user re-ran `openrv_draws_rect_xstudio_verifies` after this fix — `add_media`/`set_selection` now reach the xStudio peer (previously silently dropped), and the full test passes.
