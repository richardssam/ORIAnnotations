## 1. Make a structural refusal reportable

- [x] 1.1 Give `insert_child` a `SENT`/`SUPPRESSED` return value using the existing `authority` vocabulary, matching `broadcast_move_child`/`broadcast_remove_child` (spec: `otio-sync-core` — "A refused structural broadcast reports that it was refused").
- [x] 1.2 Return `SUPPRESSED` from the role refusal and from the no-broadcast paths; return `SENT` only when the message actually goes out.
- [x] 1.3 Test that every existing caller of `insert_child` that ignores the return value behaves exactly as before — no signature break, no changed side effects. Verified via the full `run_tests_core.sh`/`run_tests_xstudio.sh` suites (every existing caller ignores the return already) rather than a bespoke test.
- [x] 1.4 Test that a role-refused structural insert reports refusal and an emitted one reports `SENT`. `tests/otio_sync/test_structure_divergence_recovery.py::test_insert_child_reports_{sent_when_broadcast,suppressed_when_role_forbids}`.

## 2. Attribute divergence

- [x] 2.1 Add `_structural_divergence_applies()` to `SyncManager`: not `_is_syncing`, `network` present, `status == STATE_SYNCED` (design D1).
- [x] 2.2 Add `_note_structure_diverged(site, reason)`: sets `structure_diverged`, records the settle deadline, logs once per site+reason run via `_log_field_strip`'s pattern rather than per refusal.
- [x] 2.3 Call it from the role and lease refusal branches of `broadcast_move_child` and `broadcast_remove_child`, gated on 2.1.
- [x] 2.4 Call it from `insert_child`'s role refusal, additionally gated on `group == authority.STRUCTURE` (design D1; spec: "A permitted annotation is not a structural divergence").
- [x] 2.5 **Gate:** confirm no other early return in the three sites marks divergence — specifically `msg is None`, `_is_syncing`, absent network, and `status != STATE_SYNCED`. The last one is what stops a rebuild re-triggering itself (design D1 table). Confirmed by inspection: `broadcast_move_child`/`broadcast_remove_child` gate on all three before the role/lease branches ever run; `insert_child` gates its own call via `_structural_divergence_applies()`.
- [x] 2.6 Test each of the three sites marks divergence on a role refusal. `test_a_role_refused_{remove_child,move_child,structural_insert}_marks_divergence`.
- [x] 2.7 Test a permitted structural broadcast does not, and that a suppressed *playback* broadcast does not. `test_a_permitted_structural_broadcast_does_not_mark_divergence`, `test_a_suppressed_playback_message_is_not_a_structural_divergence`.
- [x] 2.8 Test `insert_child(group=ANNOTATION)` refused by role does not mark divergence — the test must fail if the group gate is removed. `test_a_role_refused_annotation_insert_is_not_a_structural_divergence` (plus the permitted-annotation counterpart).
- [x] 2.9 Test a lease refusal marks divergence with `ownership_enforcement_enabled()` on, and that nothing marks it when the switch is off (design D5). `test_a_lease_refusal_marks_{divergence_when_ownership_enforced,nothing_when_ownership_disabled}`.
- [x] 2.10 Test a patcher no-op (`msg is None`) and a `_is_syncing` remote apply both leave the peer undiverged. `test_a_patcher_noop_does_not_mark_divergence`, `test_a_remote_apply_does_not_mark_divergence`.

## 3. Re-enter the join path from `STATE_SYNCED`

- [x] 3.1 Record why a state request was sent (`state_request_reason`: `"join"` / `"recovery"`, public — a host integration's `on_synced` callback needs to read it, see 6.4) in `request_state()` (design D3).
- [x] 3.2 Split the 5 s timeout branch in `tick()`: a join request keeps today's behaviour (clear `master_guid`, → `STATE_DISCOVERING`); a recovery request keeps `master_guid`, returns to `STATE_SYNCED`, and emits `structure_recovery_failed`.
- [x] 3.3 Clear `structure_diverged` only from an `apply_snapshot` that a recovery requested — never on the request, never on the timeout (spec: "Recovery clears the divergence only on success").
- [x] 3.4 Update `request_state()`'s and `on_synced`'s docstrings: both are now reachable mid-session, and `on_synced` fires once per join *including a re-join in place* (design D2).
- [x] 3.5 Test a synchronised peer's re-request is sent, and that no departure or any other message is emitted by the attempt (spec: "SHALL NOT be reported to other peers as having left"). `test_a_synced_peer_can_request_state_without_leaving`.
- [x] 3.6 Test deltas arriving during a re-requested snapshot are buffered and replayed, on the same terms as a join (reuse `test_delta_replay.py`'s fixtures). `test_deltas_during_a_rerequest_are_buffered_and_replayed`.
- [x] 3.7 Test an unanswered re-request returns the peer to `STATE_SYNCED`, leaves `master_guid` intact, and does not enter discovery. `test_an_unanswered_recovery_request_returns_to_synced_not_discovering` (plus `test_an_unanswered_join_request_still_falls_back_to_discovering` pinning the unchanged join behaviour).
- [x] 3.8 Test a master serving a state request from an already-joined peer answers it identically to a joiner's request. `test_a_master_serves_a_mid_session_request_identically_to_a_join`.

## 4. Coalesce and schedule recovery

- [x] 4.1 Add `DIVERGENCE_SETTLE` (0.5 s) and `RECOVERY_COOLDOWN` (5 s) as named module constants with the reasoning from design D4/D5 in their comments.
- [x] 4.2 Start recovery from `tick()` only: divergence settled, no request in flight, cooldown elapsed, `status == STATE_SYNCED`. Never from the refusal itself (design D4 — reentrancy, not just coalescing).
- [x] 4.3 Test several refusals inside the settle window produce exactly one `STATE_REQUEST`. `test_several_refusals_in_the_settle_window_trigger_one_request`.
- [x] 4.4 Test a divergence raised while `status == STATE_JOINING` survives the rebuild and triggers a second one on a later tick (spec: "Divergence during a rebuild is not lost"). `test_divergence_during_a_rebuild_is_not_lost` (white-box: calls `_note_structure_diverged` directly, since none of the three public sites can reach it mid-rebuild by construction — see design D1 — but the survive-and-retrigger bookkeeping in `apply_snapshot`/`tick` needs covering regardless of how it fires in production).
- [x] 4.5 Test sustained lease contention produces recoveries paced by `RECOVERY_COOLDOWN` rather than a rebuild loop. `test_sustained_contention_is_paced_by_the_cooldown`.
- [x] 4.6 Test a diverged peer with no `master_guid` sends nothing, keeps its timelines, stays diverged, and recovers once a master appears (spec: "No peer can serve state"). `test_no_master_stays_diverged_and_recovers_once_one_appears`.
- [x] 4.7 Test a diverged peer that is itself master sends no request and reports unrecoverable (design D8). `test_a_diverged_master_reports_unrecoverable_without_sending`.

## 5. Project and render the condition

- [x] 5.1 Derive `structure_divergence` ∈ `None | "recovering" | "unrecoverable"` in `session_state_snapshot()`, alongside `driverless` and for the same reason — one derivation, two panels (design D7).
- [x] 5.2 Expose it on `SessionStateModel` in `ui_model.py` as a notifying `Property`, following `joinConfirmationOutcome`.
- [x] 5.3 Extend `tests/otio_sync/test_session_state_snapshot.py` for all three values, including the synchronised case reporting `None`. `test_snapshot_reports_{no_structure_divergence_when_synchronised,recovering,unrecoverable}`.
- [x] 5.4 Show both conditions in `python/otio_sync_core/qml/SessionStatePanel.qml`, visibly distinguishable from each other (spec: `session-state-ui`).
- [x] 5.5 Show the same two conditions in `xstudio_plugin/ori_sync/qml/ORISyncPlugin.1/SessionStatePanel.qml`.
- [x] 5.6 Present the condition as a fact about this peer's own session — "your content may not match the session" — not as a peer or session error.
- [x] 5.7 Log the divergence and each recovery outcome in a form greppable across a merged peer log (`debug/merge_sync_logs.py`). Satisfied for free: every divergence/recovery log line goes through the same `_log()`/`_log_field_strip()` calls the rest of the module uses, which `merge_sync_logs.py` already parses generically.

## 6. Survive the rebuild in each host

- [x] 6.1 Read `docs/xstudio_constraints.md` and `docs/openrv_constraints.md` before touching either plugin.
- [x] 6.2 Add `StructureSyncController.reload_existing_timelines()` (`structure_sync.py`): for each guid in `plugin.manager.timelines` already present in `plugin._sync_playlists`, reload a sequence timeline via `xs_timeline.load_otio(otio_str, clear=True)` mirroring `apply_remote_remove_child`'s sequence branch (design D2a).
- [x] 6.3 **Gate, resolved:** flat-playlist timelines are reachable through the structural sites — `poll_flat_playlist_new_media`/`poll_flat_playlist_reorders` call `insert_child`/`broadcast_remove_child`/`broadcast_move_child` on both master and client, and the proposal's own motivating incident (`'flat playlist deleted media: seq_A removed'`) is one. Covered in `reload_existing_timelines()` by reusing `apply_remote_clip_insert`/`apply_remote_remove_child`/`apply_flat_playlist_move` against the guid-set diff (design D2a).
- [x] 6.4 Wire xStudio's `_on_synced()` to enqueue `reload_existing_timelines()` in addition to `load_timelines`, gated on `self.manager.state_request_reason == "recovery"` — never from the general `add_timeline` remote-apply path, which must keep calling only `do_load_timelines()` (design D2a).
- [x] 6.5 Confirm every path inside `reload_existing_timelines()` carries suppress-window/annotation-scope handling: the sequence branch sets `_structural_mutation_suppress_until` and uses `remote_structural_apply_scope()` directly; the flat branch inherits both from the existing appliers it calls — without either, the rebuild is read back as fresh user edits and re-diverges (design Risks, first entry).
- [x] 6.6 **Gate, revised — found live:** the `_rv_updating` and `_sequence_settle_until` reasoning below held, but static reading missed a third thing: Pass 3 of `_rebuild_rv_session()` (`sequence_sync.py`, `len(timelines) > 1` branch) created a brand-new `RVSequenceGroup` unconditionally for every multi-timeline sequence, never checking `_rv_node_to_timeline_guid` for one already mapped to that guid. Harmless on a fresh join (nothing exists yet); on a recovery rebuild — the first time anything called `_rebuild_rv_session()` twice on a live session — it duplicated every sequence RV already had, and view-follow logic then resolved the wrong copy, breaking xStudio's click-to-select. Fixed in `sequence_sync.py` Pass 3: reuse the existing node (`setNodeInputs` + rename) when `_rv_node_to_timeline_guid` already maps the guid to a live node, matching the `elif len(timelines) == 1` branch immediately below it, which already did this (design D2b). `_rv_updating` and `_sequence_settle_until` needed no changes — confirmed unconditional as originally reasoned:
  - `_on_synced` wraps `rebuild_rv_session()` in `self._rv_updating = True`/`finally: False` (`plugin.py:328-337`) regardless of join vs. recovery.
  - `_rebuild_rv_session()` arms `_sequence_settle_until[view] = time.time() + 5.0` for every sequence it maps (`sequence_sync.py:1850/1854`), honoured by `_check_sequence_reorders` (`sequence_sync.py:1206-1213`); `_poll_new_sequences` can't mistake a rebuilt sequence for new since `rebuild_rv_session()` populates `_rv_node_to_timeline_guid` for it first.
- [x] 6.7 Confirm the recovery rebuild runs on the poll thread in both plugins, per each plugin's threading invariant. xStudio: `reload_existing_timelines()` runs via `_execute_command`, poll-thread-only like every other queued command. RV: `on_synced` fires from inside `apply_snapshot`, called from `tick()`, already poll-thread-only.
- [x] 6.8 Verify RV's deferred `confirm_join_state` reports on the recovery snapshot rather than the original join, via the `join_generation` bump in `apply_snapshot` (design D2). Confirmed by construction: `apply_snapshot` bumps `join_generation` on every call regardless of `state_request_reason`, and `_on_synced` captures `_join_generation = self.sync_manager.join_generation` synchronously before scheduling the deferred check — unconditional, no recovery-specific branch needed.
- [x] 6.9 Test `reload_existing_timelines()` against a diverged flat playlist: `tests/xstudio_plugin/test_structure_divergence_recovery_reload.py` covers membership (a locally-deleted clip restored, a locally-added phantom removed), reorder-only reconciliation, and the skip for a timeline this peer has no playlist for. The sequence branch is a near-verbatim reuse of `apply_remote_remove_child`'s already-relied-upon sequence path (design D2a) rather than new logic, and its coverage comes from the live tasks below (7.3–7.5) rather than a unit test — faking `prepare_otio_for_load`/`bootstrap_mapping`/`remote_structural_apply_scope` to unit-test it would exercise the fakes more than the code.

## 7. Verification

- [x] 7.1 Run `./run_tests_core.sh` and `./run_tests_xstudio.sh`; record counts (see `docs/testing.md` for the rv-stub collision and the known flaky test). 2026-08-16: `./run_tests_core.sh` — 695 passed, 4 subtests passed. `./run_tests_xstudio.sh` — 101 passed.
- [x] 7.2 Reinstall the RV package (`rvplugin/ori_sync/reinstall.csh`) and confirm from the startup banner which copy RV loaded, before any live test.
- [x] 7.3 Live, reproducing the 2026-08-13 case: xStudio viewer deletes a clip while a driver holds structure. Confirm the refusal is logged, one rebuild follows, the clip returns, and the panel showed the condition.
  - 2026-08-16 attempt 1 (RV as the restricted `viewer` peer, deleting from a sequence xStudio held structure on): refusal logged, one recovery cycle ran, the clip came back — but exposed the Pass 3 duplicate-sequence bug (design D2b) and a knock-on xStudio-click-doesn't-select-in-RV symptom. Fixed.
  - 2026-08-16 attempt 2 (after the D2b fix): duplicate sequences gone, click-to-select presumably follows again (not independently re-checked yet) — but two recovery cycles both settled on the wrong frame afterward (`expected ~163.0, got 100.0`; next cycle `expected ~131.0, got 0.0`), diagnosed as a `setNodeInputs`-triggered async frame reset racing the existing single-tick settle window. Fixed via D2c (a recovery-only second `_apply_playback` one tick later, before confirming).
  - **Also found, not fixed (out of scope for this change):** the plain join itself showed an unrelated confirmation mismatch in the same session (`[JOIN-CONFIRM] mismatched: unexpected timeline f5e35c8d ('default')`) — a different symptom (wrong timeline, not a frame race), on the path `post-join-state-confirmation` already verified. Worth its own investigation; not folded into D2c since D2c's fix is confined to a recovery-triggered synced firing and there is no evidence tying the two together.
  - 2026-08-16 attempt 3 (xStudio, not RV, as the restricted client peer — deleting from `Sequence 1`, which it had loaded via its own join snapshot): nothing was refused, nothing diverged, nothing recovered — the client never even noticed the local deletion. Traced to `do_load_timelines()`'s sequence-creation branch missing a `subscribe_timeline_item_events(guid, xs_timeline)` call that its sibling branches (flat playlists, and the other sequence-creation path at `timeline_build.py:405`) already make — with no periodic backstop for this specific subscription, a peer that loads a sequence this way can never detect its own edits to it, structure-divergence-recovery or otherwise (design D2d). This is a pre-existing `structure-events` gap this change's live testing happened to be the first thing to exercise, not something introduced here. Fixed.
  - 2026-08-16: confirmed working after D2b/D2c/D2d, user-verified.
- [x] 7.4 Live: same peer deletes a multi-clip selection. Confirm one `STATE_REQUEST`, not one per clip. 2026-08-16: user-verified working.
- [x] 7.5 Live: viewer reorders clips (the silent case — no error is surfaced). Confirm divergence is still detected and the order is restored. 2026-08-16: user-verified working.
- [x] 7.6 Live: kill the master mid-recovery. Confirm the viewer stays in the session, keeps its content, and the panel shows the unrecoverable condition. 2026-08-16: user-verified working (RV diverged, master killed before the recovery request resolved, panel showed recovering then unrecoverable at the timeout).
- [x] 7.7 Live: confirm a reviewer drawing annotations under a structure-forbidding role never trips recovery over a full session. 2026-08-16: user-verified — a session's worth of annotating produced no `structure diverged` line and no false-positive recovery. (A separate, unrelated position-echo-loop and a delete-not-detected issue surfaced afterward — see project notes; neither involved a divergence/recovery cycle, confirmed via `debug/merge_sync_logs.py --view diverge` showing no split and no `Rebuilding RV session` beyond the initial join.)
- [x] 7.8 Sample free memory and swap alongside the live runs, so latency from paging is not misread as a timing race. 2026-08-16: `vm_stat`/`sysctl vm.swapusage` sampled post-hoc — ~6.35GB free RAM, ~6.8GB/8GB swap used (historical, not necessarily live-pressure). None of the 7.3–7.7 timings showed the signature of paging-inflated latency: every measured settle-to-request gap landed within ~10-20ms of the coded `DIVERGENCE_SETTLE`, not the multi-hundred-ms-to-second inflation swap thrashing previously produced (`feedback_suite_results_need_memory_check`). No re-test needed on this basis.
- [x] 7.9 Revisit `DIVERGENCE_SETTLE` and `RECOVERY_COOLDOWN` against what 7.3–7.5 measured, and record the values chosen (design Open Questions). Measured refusal→`STATE_REQUEST` gaps: 0.510s (RV, task 7.3) and 0.522s (xStudio, task 7.5's reorder) — both ~10-22ms over the coded 0.5s `DIVERGENCE_SETTLE`, i.e. exactly one tick-loop pass past the deadline. No case needed the settle window widened (no observed double-trigger from a burst arriving late) or narrowed (no perceptible lag reported). `RECOVERY_COOLDOWN` (5s) wasn't independently stressed live — no test hammered lease contention repeatedly — but 7.6's no-master path showed no retry storm, consistent with it being adequate; only unit-level pacing coverage (`test_sustained_contention_is_paced_by_the_cooldown`) backs it beyond that. **Decision: keep both values as originally reasoned in design.md D4/D5.** Recorded in design.md's Open Questions.
