## 1. Core lease mechanism (commit 1a — dark, enforcement disabled)

- [x] 1.1 Add a category → lease-channel table to `authority.py` beside `BROADCAST_CATEGORIES` (position, display, structure channels; visibility and annotation map to no channel)
- [x] 1.2 Add per-channel `OwnershipLease` state to `SyncManager` (`owner_guid`, `claim_ts`, `deadline`, `pending_claimant`) — plus a `confirmed` flag; see manager.py `OwnershipLease` docstring for why (reconciles D2 convergence with "owner holds until idle")
- [x] 1.3 Define `CLAIM_OWNERSHIP` as a typed protocol message (`category`, `peer_guid`, `claim_ts`), registered in the message registry
- [x] 1.4 Define `RELEASE_OWNERSHIP` as a typed protocol message (`category`, `peer_guid`), registered in the message registry
- [x] 1.5 Implement claim resolution as a pure function of `(claim_ts, peer_guid)` pairs — earliest `claim_ts` wins, lower `peer_guid` breaks exact ties — mirroring `elect_host_guid`'s shape
- [x] 1.6 Implement local-clock lease expiry: each peer sets `deadline = local_monotonic_now + T` on receipt of a claim or a category broadcast; no deadline value crosses the wire
- [x] 1.7 Implement expiry handling: transfer to `pending_claimant` if one exists (applying the same D2 ordering rule to pending claims), else mark the channel free
- [x] 1.8 Add `strip_position_fields` to `authority.py`, mirroring `strip_visibility_fields`
- [x] 1.9 Add `SyncManager._enforce_position`, mirroring `_enforce_visibility`; call it beside `_enforce_visibility` inside `broadcast_playback_state` so visibility and position fields are stripped independently
- [x] 1.10 Gate `broadcast_display_state`'s fields on the display channel, separately from `_enforce_position`'s position channel
- [x] 1.11 Gate the structural broadcast methods (add/remove/replace/rename timeline, structural `MOVE_CHILD`/`REMOVE_CHILD`) on the structure channel. **Scope note**: `SET_PROPERTY` and `INSERT_CHILD` are *not* gated here — today they have no core-side authority check at all (unlike the `broadcast_*` methods); their only gate is the `is_master` check at the RV plugin's call site (`sequence_sync.py:404`). Replacing that call-site check with a structure-lease check is task 2.5, consistent with where the existing `is_master` gate already lives.
- [x] 1.12 Add `manager.claim_category(category)`, applying the local claim to this peer's own lease state via the same resolution rule used for received claims, and broadcasting `CLAIM_OWNERSHIP`
- [x] 1.13 Add `manager.release_category(category)`, broadcasting `RELEASE_OWNERSHIP` and freeing (or transferring) the lease immediately rather than waiting for expiry
- [x] 1.14 Add a `broadcast_ownership` section to `StateSnapshot`: per-channel `owner_guid` + `remaining_ms`, omitted when a channel has no owner
- [x] 1.15 Add an `adopt_ownership()` operation (mirroring `adopt_host()`) that ignores an absent/`None` section on receipt rather than clearing local lease state
- [x] 1.16 Add `ORI_BROADCAST_OWNERSHIP` kill switch, read per call (not cached at import), defaulted to **disabled** for this commit
- [x] 1.17 Add any new core module to `rvplugin/ori_sync/makepackage.csh`'s vendoring list in the same commit that introduces it; prefer extending `authority.py` over adding a module — no new module was added (extended `authority.py`/`manager.py`/`protocol_messages.py`, all already vendored), so the list needed no change
- [x] 1.18 Expose per-channel lease state (owner, remaining time) on both test-inspector hooks, alongside `is_host`/`host_guid`, and add it to the runner's `ignore_keys`
- [x] 1.19 Unit tests: concurrent-claim convergence (D2), expiry-triggered transfer to a pending claimant (D3), late-join adoption from `STATE_SNAPSHOT` (D6), and an old-style snapshot with no `broadcast_ownership` section leaving local leases unchanged — `tests/otio_sync/test_broadcast_ownership.py`, 31 tests, all passing; full `tests/otio_sync/` suite re-run clean (same 11 pre-existing unrelated failures as on `main`)

## 2. Plugin integration and soak (commit 1b — enforcement on by default)

- [x] 2.1 Flip `ORI_BROADCAST_OWNERSHIP` to enabled by default, switch still reverting behaviour completely when set to disable — required updating three pre-existing test files (`test_broadcast_authority.py`, `test_protocol_messages.py`, `test_send_side_guard.py`) that predate ownership to explicitly disable it, since they exercise other mechanisms without ever claiming a lease
- [x] 2.2 RV: convert `_rv_updating` into a depth-counted reentrant context manager so a remote apply nested inside another remote apply keeps suppression active until the outermost scope exits — implemented as a property with increment/decrement semantics on assignment, so all ~30 existing `= True`/`= False` call sites keep working unchanged; also added a `with self._updating():` form for new code
- [x] 2.3 RV: wire `claim_category("position")` into `playback_sync.py`'s input-driven scrub/play/stop paths only (never from the apply-scope guard) — single choke point in `_broadcast_playback()`, already guarded by `_rv_updating`
- [x] 2.4 RV: wire `claim_category("display")` into `display_sync.py`'s input-driven paths — in `_broadcast_display_state()`, gated the same way
- [x] 2.5 RV: replace the `is_master` gate at `sequence_sync.py:404` with a structure-lease check; wire `claim_category("structure")` into input-driven structural edit paths, including the master's own rebuild path — done for `check_otio_snapshots` (the OTIO path this task names) and, since the lease now also gates the native (non-OTIO) `broadcast_*` methods that previously had no authority check at all, added matching claims to the native poll paths (`_check_sequence_reorders`, `_poll_new_sequences`, `_poll_sequence_renames`, `_poll_deleted_sequences`, `add_clip_from_path`) so they aren't permanently suppressed. Also fixed an ordering hazard: local OTIO bookkeeping (`_otio_last_export`) is now only marked "sent" when the broadcast actually returns `SENT`, so a suppressed push retries instead of being silently dropped
- [x] 2.6 xStudio: stamp `last_remote_apply[category] = now` on each controller when a remote message in that category is applied — centralised as `ORISyncPlugin.stamp_remote_apply()`, called from `apply_playback_state`, `apply_display_state`, the three structural `apply_remote_*` methods, and once at the top of `_handle_manager_event` for the remaining structural actions (add/remove/replace timeline, rename)
- [x] 2.7 xStudio: make `claim_category()` a no-op within the ~0.3s horizon after the matching `last_remote_apply[category]` stamp, per controller — centralised as `ORISyncPlugin.claim_lease()` / `_CLAIM_HORIZON_S = 0.3`, used by all three channels rather than duplicated per controller
- [x] 2.8 xStudio: wire `claim_category("position")` into `playback_sync.py`'s input-driven paths — via `claim_lease()` in the `"broadcast_playback_state"` command handler (`ori_sync_plugin.py`), the single poll-thread choke point for all position broadcasts
- [x] 2.9 xStudio: wire `claim_category("display")` into `display_sync.py`'s input-driven paths — in `poll_and_broadcast_display()`
- [x] 2.10 xStudio: wire `claim_category("structure")` into `structure_sync.py`'s input-driven paths — added at all 14 structural broadcast/`insert_child` call sites across the `poll_*` methods
- [x] 2.11 xStudio: replace `annotation._reload_suppress_until`'s wall-clock window with an apply-scope guard around remote structural applies, sized to the apply's actual duration (not a fixed timeout) — split into `_reload_apply_depth` (depth-counted, covers the apply itself for however long it takes) plus a short `_reload_residual_until` window armed only after a successful apply (covers xStudio's async echo callbacks arriving after return); the single non-structural caller (`set_annotation` echo suppression) now calls `arm_reload_residual()` directly
- [x] 2.12 Both hosts: confirm a suppressed broadcast never queues its payload — verified by design review rather than new code: RV always re-reads current state fresh per input event, xStudio enqueues a freshly-captured payload per event and never retries a dropped one; neither plugin has (or gained) any "remember and replay" mechanism
- [x] 2.13 Add a deliberately-contended two-peer test case to `sync_test/` — both peers driving position (and separately, structure) at once, asserting the lease transfers and converges. Added `concurrent_commands` (fires one command per named app from its own thread, so requests genuinely overlap) and `expect_ownership_convergence` (polls `/state`'s `broadcast_ownership` until every peer agrees) as new runner actions, plus `contended_position_scrub` and `contended_structure_add_media` in `sync_tests.yaml`. **UNVERIFIED** — this environment has no live RV/xStudio to run the harness against; see 2.15
- [x] 2.14 Update the protocol doc generator's config/classes so `CLAIM_OWNERSHIP`, `RELEASE_OWNERSHIP`, and the `broadcast_ownership` snapshot section are documented — added to `docs/config.yml`; ran the generator end-to-end and confirmed both messages and the field render in the output HTML
- [x] 2.15 Soak: **partially done, 2026-08-08.** A live two-app RV+xStudio manual soak ran after catching and fixing a real deployment gap (RV was loading a stale pre-broadcast-ownership rvpkg install — see below). Confirmed working live: xStudio sends real `CLAIM_OWNERSHIP` traffic; RV, once reinstalled, participates; annotations and scrubbing worked correctly from RV as a non-host client; the visibility/position split behaved exactly per spec (RV's clip selection did not propagate — correct, RV is not host — but its frame position did). No lease-transfer misbehavior, no new tracebacks attributable to this change, and no evidence the default `T` values need retuning. **Not done**: the scripted `sync_test/sync_tests.yaml` suite was not run — in particular the two new contended cases (`contended_position_scrub`, `contended_structure_add_media`) and the `concurrent_commands`/`expect_ownership_convergence` runner actions they depend on remain unverified against real apps. Treat those as an untested first draft until someone runs the suite.

  **Deployment gap found and fixed during the soak**: RV loads its plugin from an installed rvpkg copy (`~/Library/Application Support/RV/Python/`), not the repo directly — the installed copy was dated before any of this session's work, so the first soak attempt exercised only xStudio's side. Fixed by running `rvplugin/ori_sync/reinstall.csh` and relaunching RV. No code change; a process note for whoever runs this suite next.

  **Follow-up opened, not part of this change**: the soak surfaced two related but out-of-scope observations. (1) A gap in `fix-visibility-authority-bypass`'s send-side coverage — a follower whose local view has silently diverged from the host's should not broadcast its now-meaningless position either, not just be pulled back on receive. Not yet written into that proposal; flagged in conversation, not the file. (2) A new "Host as a lease, not a static election, when candidates are equally ranked" open consideration added to `session-roles/proposal.md` — host election has no idle-expiry handoff today, unlike position/structure.

## 3. Guard removal (commit 1c — blocked, pure removal)

- [ ] 3.1 Confirm `fix-visibility-authority-bypass` has closed before starting this group
- [ ] 3.2 Confirm 2.13's contended case positively exercised each guard below under contention and found it silent (D5 exit criterion — absence of firings in an uncontended soak does not satisfy this)
- [ ] 3.3 Delete `playback._playback_apply_suppress_until` (xStudio)
- [ ] 3.4 Delete `playback._last_applied_frame` / `_last_polled_frame` (xStudio)
- [ ] 3.5 Delete `playback._last_received_frame` (xStudio)
- [ ] 3.6 Delete `playback._local_scrub_active_until` (xStudio)
- [ ] 3.7 Remove the throttled scrub flush's stale-position re-check (xStudio)
- [ ] 3.8 Delete `playback._loop_mode_apply_suppress_until` (xStudio)
- [ ] 3.9 Delete `structure._structural_mutation_suppress_until` (xStudio)
- [ ] 3.10 Remove the now-dead remnant of the `is_master` gate at `sequence_sync.py:404`, leaving only the structure-lease check added in 2.5 (RV)
- [ ] 3.11 Leave untouched: `playback._applying_pinned_mode`, `_rv_updating`, the `_reload_suppress_until` replacement, `_is_syncing`, and the `source_guid` filter — these are apply-scope/fundamental guards, not retired by this change
- [ ] 3.12 Leave untouched: `_ignore_annotations_until`, `_our_bookmark_uuids`, `_selection_broadcast_suppress_until`, `_applied_clip_echo_guid`/`_until`, `_local_view_action_until` — annotation stays multi-writer and the visibility-only guards are `fix-visibility-authority-bypass`'s to retire, not this change's
- [ ] 3.13 Re-run the full `sync_test/` suite plus a live two-app soak after deletion to confirm no regression
- [ ] 3.14 Cross-reference `docs/visibility_authority_guards.md` to note which guards retired under this change, keeping its warning against re-adopting the falsified "zero firings" premise intact for the guards that remain
