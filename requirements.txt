# ORIAnnotations dependencies (direct imports only).
# Transitive dependencies are resolved automatically by pip.
#
# NOTE: PySide6/shiboken6, the `rv` module, and the `xstudio` module are
# provided by their host applications (OpenRV / xStudio) and are intentionally
# NOT listed here.

# --- Core ---
OpenTimelineIO @ file:///Users/sam/git/OpenTimelineIO
pika==1.3.2            # RabbitMQ sync transport
numpy==2.2.1
PyYAML==6.0.2

# --- Web sync viewer (sync_viewer/server.py) ---
fastapi==0.128.0
uvicorn==0.37.0

# --- Tooling / testchart / utilities ---
pillow==11.1.0         # testchart image generation
requests==2.32.3       # test_media/stem_download.py

# --- Docs (docs/) ---
Sphinx==8.1.3
sphinx-mermaid==0.0.8
mistune==3.1.1
# black is used optionally by docs/doc_generator.py for code formatting;
# uncomment if you want formatted code samples in the docs.
# black

# --- Tests ---
pytest==9.0.2
